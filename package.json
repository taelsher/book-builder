{
  "name": "networkstate-blume-rebuild",
  "version": "1.0.0",
  "description": "Tier 1 structural/visual/navigational rebuild of thenetworkstate.com on Blume — local test build, stub prose.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "dev": "blume dev",
    "build": "blume build",
    "preview": "blume preview",
    "check": "blume check",
    "validate": "blume validate",
    "editor": "[ -d editor/node_modules ] || npm --prefix editor install; npm --prefix editor run dev"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "blume": "1.3.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2"
  },
  "pnpm": {
    "patchedDependencies": {
      "blume@1.3.1": "patches/blume@1.3.1.patch"
    }
  }
}
